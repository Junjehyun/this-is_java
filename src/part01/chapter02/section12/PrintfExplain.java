package part01.chapter02.section12;

public class PrintfExplain {
    /**
     * 우리는 지금까지 표준 출력 장치인 모니터(명령 프롬프트, 터미널, 콘설)에 값을 출력하기 위해 
     * System.out.println() 메소드를 사용했다.
     * 
     * 괄호() 안에 리터럴을 넣으면 리터럴이 그대로 출력되고, 변수를 넣으면 변수에 저장된 값이 출력 되었다.
     * 
     * System. + out. + println(리터럴 또는 변수);
     * 
     * 시스템으로 + 출력하는데 + 괄호 안의 내용을 출력하고 행을 바꿔라.
     * 
     * 출력 방법에 따라 println() 이외에도 다음과 같이 print(), printf() 메소드를 사용할 수 있다.
     * 
     * println(내용); 괄호 안의 내용을 출력하고 행을 바꾼다.
     * print(내용); 괄호 안의 내용을 출력하고 행을 바꾸지 않는다.
     * printf("형식문자열", 값 또는 변수); 형식문자열에 따라 값 또는 변수를 출력한다.
     * 
     * PrintfExample.java 참조
     */
}
